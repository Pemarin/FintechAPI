[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    --tb=short
    -ra
    -q

# Marcadores customizados
markers =
    unit: testes unitários sem dependências externas
    integration: testes de integração com FastAPI TestClient
    slow: testes lentos (skip com -m "not slow")
